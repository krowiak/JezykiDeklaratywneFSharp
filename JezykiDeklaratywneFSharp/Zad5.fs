module Zad5
open System.Numerics

let a = new Complex(1.0, 1.0) / new Complex(1.0, -1.0)
let b = new Complex(0.0, 2.0) / new Complex(1.0, 1.0)
let c = new Complex(4.0, -3.0) / new Complex(4.0, 3.0)
let d = Complex.Sqrt(new Complex(-3.0, -4.0))
let e = Complex.Sqrt(new Complex(8.0, 6.0))
let f = Complex.Pow(new Complex(1.0, 1.0), 10.0)
let g = Complex.Pow(new Complex(2.0, sqrt 12.0), 5.0)
let h = Complex.Pow(new Complex(-1.0, 0.0), -4.0)